@*
    @if (IsSignin && signin.IsInRole("customer"))
    {
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/public/search-products">
                <span class="oi oi-browser" aria-hidden="true"></span> Товары
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/user/orders">
                <span class="oi oi-basket" aria-hidden="true"></span> Заказы
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/user/basket">
                <span class="oi oi-basket" aria-hidden="true"></span> Корзина
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/booking/list-bookings">
                <span class="oi oi-browser" aria-hidden="true"></span> Записаться на ремонт
            </NavLink>
        </div>
    }
*@
@inject AppBuilder builder
@inherits BaseComponent
@code
{
    public Dictionary<string, string> MenuModel { get; set; }
    protected override Task OnInitializedAsync()
    {
        return base.OnInitializedAsync();
        this.MenuModel = builder.CreateNavMenu<CustomerMenu>();
    }
}
<NavMap Model="@MenuModel"></NavMap>