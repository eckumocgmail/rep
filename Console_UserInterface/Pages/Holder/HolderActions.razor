@code
{
    [Parameter]
    public int HolderId { get; set; }
}
<NavModel Model="@((new AppBuilder()).GetNavModel(GetType().Namespace, new Dictionary<string, string>(){ {"HolderId", HolderId.ToString() }} ))"></NavModel>