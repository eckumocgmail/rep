@using Console_UserInterface.AppUnits;
@code
{
    [Parameter]
    public int HolderId { get; set; }
}
<NavModel Model="@(AppBuilder.CreateNavMenu( GetType().Namespace, new Dictionary<string, string>(){ {"HolderId", HolderId.ToString() }} ))"></NavModel>