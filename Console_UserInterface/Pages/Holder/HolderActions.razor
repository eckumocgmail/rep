@using Console_UserInterface.AppUnits;
@using Console_UserInterface.Services.App;
@code
{
    [Parameter]
    public int HolderId { get; set; }
}
<NavMap Model="@(AppBuilder.CreateNavMenu( GetType().Namespace, new Dictionary<string, string>(){ {"HolderId", HolderId.ToString() }} ))"></NavMap>